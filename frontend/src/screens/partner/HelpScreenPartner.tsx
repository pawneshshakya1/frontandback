import React from 'react';
import { View, Text, StyleSheet } from 'react-native';
import { HelpScreen as BaseHelpScreen } from '../main/HelpScreen';
import { COLORS } from '../../theme/colors';

export const HelpScreenPartner = (props: any) => {
  return <BaseHelpScreen {...props} />;
};
